# OCR Dataset

This directory contains the dataset for the Optical Character Recognition (OCR) component.

## Structure
- Place raw images in an `images/` subdirectory (recommended).
- Place ground truth text annotations in a `annotations/` subdirectory or a shared `labels.json` file.

## Purpose
Used for testing and validting the OCR extraction capabilities.
